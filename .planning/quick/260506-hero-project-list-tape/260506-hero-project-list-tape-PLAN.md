# Quick Task 260506: Hero Project List Tape CTA

**Date:** 2026-05-06
**Status:** Executed inline because `gsd-sdk` is not available in this runtime.

## Goal

Turn the passive hero `Scroll` label into a real project-list CTA without making it feel like a standalone button.

## Tasks

1. Convert the hero bottom strip into a single link to the future `projects.html` page.
2. Make the whole horizontal strip behave like a sliding tape on hover/focus.
3. Reveal a rust background panel behind the tape, keeping the gesture notable but restrained.
4. Add ES/EN i18n copy and accessible labels.
5. Verify JSON validity, CSS diff sanity, desktop hover, and mobile layout.

## Files

- `index.html`
- `styles/main.css`
- `content/es.json`
- `content/en.json`
